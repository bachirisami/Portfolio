# Portfolio

This is my personal portfolio. It's a React + TypeScript
site with a bit of a code-editor look to it

## Running it yourself

```bash
npm install
npm run dev
```

That's it, it'll open on localhost. `npm run build`.

## Built with

React, TypeScript, Vite, React Router, Bootstrap.
